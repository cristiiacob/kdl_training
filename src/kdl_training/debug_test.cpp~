#include <ros/ros.h>
#include <ros/console.h>

int main(int argc, char **argv)
{
	ros::init(argc, argv,"debug_test");
    	ros::NodeHandle nh;
	ROS_INFO("INFO 1");
	ROS_INFO("INFO 2");
	ROS_DEBUG("DEBUG 1");
	ROS_DEBUG("DEBUG 2");
	ROS_ERR("ERROR 1");
	ROS_ERR("ERROR 2");

	ros::spinOnce();

    return 0;
}
